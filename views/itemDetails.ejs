<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=	" />
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <link rel="stylesheet" href="css/defaultPage.css" />
    <link rel="stylesheet" href="css/gridDefault.css" />
    <link rel="stylesheet" href="css/itemDetails.css">
    <title><%= title %></title>
  </head>
  <body class="mainContainer">
    <div class="header"><%- include("includes/header",{categorys: categorys}) %></div>
    <div class="main">
      <img src="images/<%=itemShown.itemName%>.jpg" alt="" class="itemImage" />
      <div class="itemDiv">
          <!-- shows items detail on page base on variable itemShown -->
          <div class="itemDetail"><p class="itemName" title="<%=itemShown.name%>"><%= itemShown.name %></p>
            <p class="price"><%= itemShown.priceUsd %>&dollar;</p>
            <div class="stars">
              <% for (let i = 0; i < itemShown.starRating; i++) { %> <% if (i+1 > itemShown.starRating) { %>
              <span class="material-symbols-outlined star">star_half</span>
              <% } else { %>
              <span class="material-symbols-outlined star">star</span>
              <% } %> <% } %>
            </div>
            <p class="description"><%= itemShown.description %></p>
            <hr>
            <% if (isLogedInn != true){ %>
              <form action="/login">
                <input type="submit" class="addCart" value="Add to Cart"></input>
                <input type="number" placeholder="Quantity">
              </form>
            <% }else{ %>
              <form action="/itemDetails?itemName=<%=itemShown.itemName%>" method="POST">
                <input type="submit" class="addCart" value="Add to Cart"></input>
                <input type="number" id="quantity" name="quantity" placeholder="Quantity" required>
              </form>
              <% }; %>
          </div>
        </a>
      </div>
    </div>
    <div class="footer"><%- include("includes/footer") %></div>
  </body>
</html>
